<% @books.each do |book| %>
  <h2><%= link_to book.title, book_path(book) %></h2>

<% end %>

<!-- 'new_book' came from a list of routes displayed by
the command 'rake route' -->
<!-- <%= link_to "Add Book", new_book_path %> -->
